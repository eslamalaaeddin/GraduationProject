package com.example.graduationproject.helper.listeners

//This listener is used to show the bottom navigation view when searching either by name or tag.
interface SearchListener {
    fun onSearchEvent()
}